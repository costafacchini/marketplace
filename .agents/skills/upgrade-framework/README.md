# Upgrade Framework

Updates the AI dev framework installation in a consumer repo. Compares the installed skills against a source copy of ai-dev-framework, copies new and updated skills, adds new skill entries to AGENTS.md without touching project-specific customizations, syncs CLAUDE.md, and validates the result.

**Trigger**: `/upgrade-framework`

See `SKILL.md` for the full skill definition used by AI agents.
