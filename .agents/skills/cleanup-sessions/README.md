# Cleanup Sessions

Deletes old session handoff documents to prevent accumulation. Use when user says "clean up sessions", "delete old sessions", or when the sessions directory has 10+ files. Sessions older than 30 days are typically stale. Scans for unlogged patterns before deletion.

**Trigger**: `/cleanup-sessions`

See `SKILL.md` for the full skill definition used by AI agents.
