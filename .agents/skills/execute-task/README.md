# Execute Task

Execute a single plan task. Loads context, branches, follows implementation steps with adaptation protocol, runs tests, updates status.

**Trigger**: `/execute-task {plan-slug}/{task-path}`

See `SKILL.md` for the full skill definition used by AI agents.
